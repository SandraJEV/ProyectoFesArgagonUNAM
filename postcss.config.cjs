// postcss.config.cjs
module.exports = {
  plugins: [
    require('tailwindcss'),  // Usa Tailwind CSS como un plugin
    require('autoprefixer'),  // Asegúrate de incluir Autoprefixer también
  ],
};
